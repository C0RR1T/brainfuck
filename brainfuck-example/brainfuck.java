public class Test {
    public static void main(String[] args) {
        cells[pointer] += 1;
        while(cells[pointer] > 0) {
            cells[pointer] = System.in.readLine();
            System.out.println(cells[pointer]);
            pointer[cells]--;
        }
    }
}